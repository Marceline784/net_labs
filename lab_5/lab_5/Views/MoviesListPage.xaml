<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:lab_5.ViewModels"
             xmlns:models="clr-namespace:lab_5.Models" 
             xmlns:local="clr-namespace:lab_5"
             x:Class="lab_5.Views.MoviesListPage"
             x:DataType="vm:MovieListViewModel"
             BackgroundColor="#FFFFFF">

    <CollectionView ItemsSource="{Binding Movies}">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:MovieViewModel">
                <SwipeView>
                    <SwipeView.RightItems>
                        <SwipeItems>
                            <SwipeItem Text="Delete"
                                       BackgroundColor="Red"
                                       Command="{Binding DeleteMovieCommand, Source={x:Static local:App.MainViewModel}}"
                                       CommandParameter="{Binding}" />
                        </SwipeItems>
                    </SwipeView.RightItems>
                    <VerticalStackLayout Padding="20" BackgroundColor="#F9F9F9">
                        <Label Text="{Binding Title}" FontSize="18" TextColor="#000000" FontAttributes="Bold"/>
                    </VerticalStackLayout>
                </SwipeView>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>